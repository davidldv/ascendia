{
  "name": "@ascendia/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node --enable-source-maps dist/index.js",
    "build": "tsc -p tsconfig.json",
    "reconcile:run": "tsx src/cron/reconcile.ts",
    "reconcile:run:prod": "node --enable-source-maps dist/cron/reconcile.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.1.0",
    "@supabase/supabase-js": "^2.90.1",
    "dotenv": "^16.4.5",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2"
  }
}
