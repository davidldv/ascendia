# Ascendia environment variables

# -----------------------------
# Expo (client) — SAFE to expose
# -----------------------------
# Expo only exposes env vars to the JS bundle when prefixed with EXPO_PUBLIC_.
# These are public by design. Do NOT put any DB passwords or service keys here.

EXPO_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
EXPO_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Optional: your Fastify backend base URL (for server-authoritative streak/missions)
# In local dev, this is typically your LAN IP so phones can reach it.
EXPO_PUBLIC_API_BASE_URL="http://localhost:3000"

# Optional: useful for feature flags / logging
EXPO_PUBLIC_APP_ENV="development"  # development | preview | production

# Backend env vars live in `apps/api/.env` (see `apps/api/.env.example`).
